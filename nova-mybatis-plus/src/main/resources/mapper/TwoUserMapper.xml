<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.nova.mybatisplus.chapter2.TwoUserMapper">

    <select id="selectByName" resultType="com.nova.mybatisplus.entity.UserDO" parameterType="String">
        select * from user where name = #{name}
    </select>

</mapper>

